name: Validator Metadata Sync

on: 
 schedule:
  - cron: 44 5 1,15 * *
 workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run:
      - run:
        env:
          X-DUNE-API-KEY: ${{ secrets.api_key}}
          namespace: ${{ secrets.namespace}}
    
   
